"""
Optional integrations with third-party frameworks.

Each integration is independently installable via an optional extra:

    pip install "context-passport[langgraph]"

Importing an integration without its underlying framework installed will
raise ImportError with a clear message indicating which extra to install.
"""
